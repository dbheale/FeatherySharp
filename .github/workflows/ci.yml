name: ci

on:
  workflow_dispatch:
  push:
    branches: [ master ] 
    tags: [ 'v*' ]
  pull_request:

jobs:
  build_test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - sdk: '8.0.x'
            tfm: 'net8.0'
          - sdk: '9.0.x'
            tfm: 'net9.0'
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET ${{ matrix.sdk }}
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ matrix.sdk }}
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/NuGet.config

      - run: dotnet --info

      # IMPORTANT: build a single TFM so 8.0 jobs don't try to restore net9.0
      - run: dotnet restore -p:TargetFramework=${{ matrix.tfm }}
      - run: dotnet build -c Release --no-restore -p:TargetFramework=${{ matrix.tfm }}
      - run: dotnet test -c Release --no-build    -p:TargetFramework=${{ matrix.tfm }}

  pack_publish:
    runs-on: ubuntu-latest
    needs: build_test
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj
            **/NuGet.config
      - run: dotnet restore
      - run: dotnet pack ./src/Feathery.Unofficial.Client/Feathery.Unofficial.Client.csproj -c Release -o ./artifacts -p:ContinuousIntegrationBuild=true
      - name: Publish NuGet
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate